openapi: "3.0.0"
info:
  title: Refs Test
  version: "1.0.0"
paths: {}
components:
  schemas:
    Order:
      type: object
      required:
        - id
        - customer
      properties:
        id:
          type: integer
        customer:
          $ref: "#/components/schemas/Customer"
        note:
          type: string
    Customer:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        email:
          type: string
